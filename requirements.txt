# --- Core Framework & Utilities ---
gradio>=4.0.0
fastapi
uvicorn[standard]
python-multipart

pyyaml
piexif
pillow
tqdm
huggingface_hub>=0.24.3

# --- Key Libraries ---
# Install torch manually
# transformers 5.x required for qwen3_5 architecture (Qwen3.5-9B models).
# If pip can't resolve >=5.0.0 from PyPI yet, install manually first:
#   pip install git+https://github.com/huggingface/transformers.git
transformers>=5.0.0
accelerate>=0.26.0
safetensors

# --- Shared Model Dependencies ---
timm
einops
sentencepiece
protobuf
bitsandbytes
scipy

# --- Model Specific Dependencies ---

# ToriiGate / Qwen2-VL
qwen_vl_utils
# flash-attn --no-build-isolation  # Qwen3-VL: Flash Attention 2 support (manual install required)
# PyQt5 (optional, used by some ToriiGate variants)

# JoyCaption / Llama
# liger_kernel # Linux only (Triton dependency)

# Pixtral / JoyTag
tiktoken

# SmolVLM2
num2words

# Miscellaneous
# spaces # HuggingFace Spaces specific helper
colorama

# WD14 Tagger
# Pinned: onnxruntime-gpu>=1.23 ships wheels linked against CUDA 13 libs
# (libcudart.so.13), which aren't present in our CUDA 12.8 runtime image and
# fail to import silently. Keep pinned to a CUDA 12.x compatible release.
onnxruntime-gpu==1.22.0
opencv-python-headless